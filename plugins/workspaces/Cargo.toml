[package]
name = "workspaces"
version = "0.1.0"
publish.workspace = true

[package.edition]
workspace = true

[dependencies]
omega = { workspace = true }
workspaces-commands = { path = "../../commands/workspaces" }

[dev-dependencies]
omega-preview = { workspace = true }
omega-proto = { workspace = true }
tokio = { version = "1", features = ["macros", "rt"] }
