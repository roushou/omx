[package]
name = "power"
version = "0.1.0"
publish.workspace = true

[package.edition]
workspace = true

[dependencies]
omega = { workspace = true }
power-commands = { path = "../../commands/power" }

[dev-dependencies]
omega-preview = { workspace = true }
tokio = { version = "1", features = ["macros", "rt"] }
